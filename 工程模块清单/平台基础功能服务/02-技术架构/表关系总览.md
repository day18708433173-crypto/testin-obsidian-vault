# SQL 表关系总览

> 平台基础功能服务 9 个 MySQL 库的核心表关系（ER），按业务域分组。字段名含 `_id` 后缀的为外键关联字段。

## 一、db_plan — 测试计划与执行记录

### 1.1 计划模板树（plan_info → sub_plan_info → plan_task）

```mermaid
erDiagram
    plan_info ||--o{ sub_plan_info : "plan_info_id"
    plan_info ||--o| plan_info_config : "plan_info_id"
    plan_info ||--o{ plan_info_scheduled : "plan_info_id"
    plan_info ||--o{ plan_info_execute_period : "plan_info_id"
    plan_info ||--o| plan_email_notice : "plan_info_id"
    plan_info ||--o{ plan_lead_user : "plan_info_id"
    plan_info ||--o{ plan_info_task_config : "plan_info_id"

    sub_plan_info ||--o{ plan_task : "sub_plan_info_id"
    plan_task ||--o{ plan_task_case : "plan_task_id"
    plan_task ||--o{ plan_task_script : "plan_task_id"
    plan_task ||--o{ plan_task_device : "plan_task_id"
    plan_task ||--o{ plan_task_strategy : "plan_task_id"
    plan_task ||--o| plan_task_template_config : "plan_task_id"

    plan_info {
        bigint id PK
        int project_id FK
        int suite_id
        varchar plan_info_name
        int plan_info_type
        int plan_info_status
    }
    sub_plan_info {
        bigint id PK
        bigint plan_info_id FK
        varchar sub_plan_name
        int order_num
        varchar execute_time
    }
    plan_task {
        bigint id PK
        bigint plan_info_id FK
        bigint sub_plan_info_id FK
        int relation_task_type
        int relation_task_id
        int order_num
    }
    plan_task_case {
        bigint id PK
        bigint plan_task_id FK
    }
    plan_task_script {
        bigint id PK
        bigint plan_task_id FK
    }
    plan_task_device {
        bigint id PK
        bigint plan_task_id FK
    }
    plan_info_config {
        bigint id PK
        bigint plan_info_id FK
    }
    plan_info_scheduled {
        bigint id PK
        bigint plan_info_id FK
        varchar scheduled_cron
    }
    plan_info_execute_period {
        bigint id PK
        bigint plan_info_id FK
    }
    plan_email_notice {
        bigint id PK
        bigint plan_info_id FK
    }
    plan_lead_user {
        bigint id PK
        bigint plan_info_id FK
    }
    plan_info_task_config {
        bigint id PK
        bigint plan_info_id FK
    }
    plan_task_strategy {
        bigint id PK
        bigint plan_task_id FK
    }
    plan_task_template_config {
        bigint id PK
        bigint plan_task_id FK
    }
```

**关系说明**：

| 父表 (1) | 子表 (N) | 关联字段 | 含义 |
|-----------|---------|---------|------|
| `plan_info` | `sub_plan_info` | `plan_info_id` | 一个计划包含多个子计划 |
| `plan_info` | `plan_info_config` | `plan_info_id` | 计划配置（1:1） |
| `plan_info` | `plan_info_scheduled` | `plan_info_id` | 计划定时执行配置（可多个 cron） |
| `plan_info` | `plan_info_execute_period` | `plan_info_id` | 计划可执行时间段 |
| `plan_info` | `plan_email_notice` | `plan_info_id` | 计划邮件通知（1:1） |
| `plan_info` | `plan_lead_user` | `plan_info_id` | 计划负责人（多人） |
| `plan_info` | `plan_info_task_config` | `plan_info_id` | 计划级别的任务配置 |
| `sub_plan_info` | `plan_task` | `sub_plan_info_id` | 子计划下的任务列表 |
| `plan_task` | `plan_task_case` | `plan_task_id` | 任务关联的用例 |
| `plan_task` | `plan_task_script` | `plan_task_id` | 任务关联的脚本 |
| `plan_task` | `plan_task_device` | `plan_task_id` | 任务关联的设备 |
| `plan_task` | `plan_task_strategy` | `plan_task_id` | 任务执行策略 |
| `plan_task` | `plan_task_template_config` | `plan_task_id` | 任务模板配置（1:1） |

### 1.2 执行记录树（execute_record → execute_record_task）

```mermaid
erDiagram
    execute_record ||--o{ execute_record_task : "execute_record_id"
    execute_record ||--o| execute_record_config : "execute_record_id"

    execute_record_task ||--o{ execute_record_task_case : "execute_record_task_id"
    execute_record_task ||--o{ execute_record_task_script : "execute_record_task_id"
    execute_record_task ||--o{ execute_record_task_device : "execute_record_task_id"
    execute_record_task ||--o| execute_record_task_config : "execute_record_task_id"
    execute_record_task ||--o{ execute_record_task_case_script_detail : "execute_record_task_id"
    execute_record_task ||--o{ execute_record_task_script_detail : "execute_record_task_id"

    execute_record {
        bigint id PK
        int project_id
        bigint plan_info_id FK
        varchar execute_record_name
        varchar plan_info_name
        int execute_status
    }
    execute_record_task {
        bigint id PK
        bigint execute_record_id FK
        bigint plan_info_id FK
        bigint sub_plan_info_id FK
        bigint plan_task_id FK
        bigint parent_id FK
        int execute_status
        int record_task_type
    }
    execute_record_task_case {
        bigint id PK
        bigint execute_record_task_id FK
    }
    execute_record_task_script {
        bigint id PK
        bigint execute_record_task_id FK
    }
    execute_record_task_device {
        bigint id PK
        bigint execute_record_task_id FK
    }
    execute_record_task_config {
        bigint id PK
        bigint execute_record_task_id FK
    }
    execute_record_task_case_script_detail {
        bigint id PK
        bigint execute_record_task_id FK
    }
    execute_record_task_script_detail {
        bigint id PK
        bigint execute_record_task_id FK
    }
```

**关系说明**：

| 父表 (1) | 子表 (N) | 关联字段 | 含义 |
|-----------|---------|---------|------|
| `execute_record` | `execute_record_task` | `execute_record_id` | 一次执行对应多个任务 |
| `execute_record` | `execute_record_config` | `execute_record_id` | 执行配置（1:1） |
| `execute_record_task` | `execute_record_task_case` | `execute_record_task_id` | 任务关联的用例 |
| `execute_record_task` | `execute_record_task_script` | `execute_record_task_id` | 任务关联的脚本 |
| `execute_record_task` | `execute_record_task_device` | `execute_record_task_id` | 任务关联的设备 |
| `execute_record_task` | `execute_record_task_config` | `execute_record_task_id` | 任务配置（1:1） |

**execute_record_task 的 parent_id 自引用**：任务节点形成树，`parent_id` 指向父任务节点。根节点 `parent_id = 0`。

### 1.3 模板 ↔ 执行快照的关联

```mermaid
erDiagram
    plan_info ||--o{ execute_record : "plan_info_id"
    plan_info ||--o{ execute_record_task : "plan_info_id"
    sub_plan_info ||--o{ execute_record_task : "sub_plan_info_id"
    plan_task ||--o{ execute_record_task : "plan_task_id"
```

执行时 `executePlanInfo()` 将模板数据快照到执行表：
- `execute_record.plan_info_id` → `plan_info.id`
- `execute_record_task.plan_info_id` → `plan_info.id`
- `execute_record_task.sub_plan_info_id` → `sub_plan_info.id`
- `execute_record_task.plan_task_id` → `plan_task.id`

### 1.4 其他辅助表

| 表 | 说明 |
|----|------|
| `plan_device` | 计划级别的设备配置（独立于 plan_task_device） |
| `script_statistic` | 脚本执行统计 |
| `plan_info_task_template_status_sync` | 任务模板同步状态跟踪 |

---

## 二、db_user — 用户与企业

```mermaid
erDiagram
    enterprise_info ||--o{ enterprise_expand : "eid"
    enterprise_info ||--o{ enterprise_user_relation : "eid"
    enterprise_info ||--o{ project_info : "eid"
    enterprise_info ||--o{ sso_config : "eid"
    enterprise_info ||--o{ audit_info : "eid"
    enterprise_info ||--o{ enterprise_auth_record : "eid"

    user_info ||--o{ enterprise_user_relation : "user_id"
    user_info ||--o{ user_role_relation : "user_id"
    user_info ||--o{ user_expand : "user_id"
    user_info ||--o{ user_login_log : "user_id"
    user_info ||--o{ user_online : "user_id"
    user_info ||--o{ user_activity : "user_id"
    user_info ||--o{ user_dept : "user_id"
    user_info ||--o{ app_user_relation : "user_id"

    project_info ||--o{ enterprise_user_relation : "project_id"
    project_info ||--o{ user_role_relation : "project_id"

    role_info ||--o{ user_role_relation : "role_id"

    enterprise_info {
        int id PK "eid"
        varchar e_short_name
        varchar e_display_name
        int e_status
    }
    user_info {
        int id PK
        varchar user_name
        varchar email
        varchar phone
        int status
    }
    project_info {
        int id PK
        int eid FK
        varchar project_name
    }
    enterprise_user_relation {
        int id PK
        int eid FK
        int user_id FK
        int project_id FK
    }
    user_role_relation {
        int id PK
        int user_id FK
        int project_id FK
        int role_id FK
    }
    role_info {
        int id PK
        varchar role_name
    }
    enterprise_expand {
        int id PK
        int eid FK
    }
    user_expand {
        int id PK
        int user_id FK
    }
    sso_config {
        int id PK
        int eid FK
    }
```

**核心关系**：

| 关系 | 关联表 | 说明 |
|------|--------|------|
| 企业 → 用户 | `enterprise_user_relation` | 多对多：用户在企业的项目中的角色 |
| 企业 → 项目 | `project_info.eid` | 一对多：企业下有多个项目 |
| 用户 → 角色 | `user_role_relation` | 用户在特定项目下拥有特定角色 |
| 用户 → 部门 | `user_dept` | 用户部门信息 |
| 用户 → 设备绑定 | `app_user_relation` | 用户与应用设备关联 |

---

## 三、db_notice — 通知与消息

```mermaid
erDiagram
    email_cfg ||--o{ email_task : "email_cfg_id"
    sms_cfg ||--o{ sms_task : "sms_cfg_id"
    qc_cfg ||--o{ qc_task : "qc_cfg_id"

    channel_cfg ||--o{ channel_event : "channel_cfg_id"
    channel_cfg ||--o{ enterprise_channel_relation : "channel_cfg_id"
    channel_event ||--o{ message_task : "event_id"

    notice_channel_cfg ||--o{ notice_event : "通过关联表"

    email_cfg {
        int id PK
        varchar host
        int port
        varchar username
    }
    email_task {
        bigint id PK
        int email_cfg_id FK
        varchar to_address
        varchar subject
        int status
    }
    sms_cfg {
        int id PK
        varchar channel_name
        varchar api_url
    }
    sms_task {
        bigint id PK
        int sms_cfg_id FK
        varchar phone
        varchar content
        int status
    }
    qc_cfg {
        int id PK
        varchar channel_name
        varchar api_url
    }
    qc_task {
        bigint id PK
        int qc_cfg_id FK
        text content
        int status
    }
    channel_cfg {
        int id PK
        varchar channel_type
    }
    channel_event {
        int id PK
        int channel_cfg_id FK
        int type
    }
    message_task {
        bigint id PK
        int event_id FK
        varchar content
        int status
    }
    notice_channel_cfg {
        bigint id PK
        varchar channel_name
    }
    notice_event {
        bigint id PK
        varchar event_name
    }
```

**关系说明**：

| 父表 | 子表 | 关联字段 | 说明 |
|------|------|---------|------|
| `email_cfg` | `email_task` | `email_cfg_id` | 邮件配置 → 待发送邮件任务 |
| `sms_cfg` | `sms_task` | `sms_cfg_id` | 短信配置 → 待发送短信任务 |
| `qc_cfg` | `qc_task` | `qc_cfg_id` | QC 配置 → QC 缺陷任务 |
| `channel_cfg` | `channel_event` | `channel_cfg_id` | 渠道 → 事件绑定 |
| `channel_cfg` | `enterprise_channel_relation` | `channel_cfg_id` | 渠道的企业级关联 |
| `channel_event` | `message_task` | `event_id` | 事件触发 → 消息任务 |
| `notice_event` | `ops_notice_event_channel_relation` | — | 运维告警事件 ↔ 渠道多对多 |

### 其他表

| 表 | 说明 |
|----|------|
| `email_templet` | 邮件模板定义 |
| `email_templet_config` | 邮件模板项目级配置（project_id） |
| `templet_cfg` | 模板配置 |
| `sms_templet` | 短信模板 |
| `http_task` | HTTP 回调待发送任务 |
| `msg_task` | IM 消息任务（钉钉/飞书/企微/Teams/JoyChat/FAW） |
| `notice_log` | 通知发送日志 |

---

## 四、db_portal — 真机门户

```mermaid
erDiagram
    portal_task ||--o| portal_version_condition : "taskid+eid+projectid"
    portal_task ||--o| task_execute_result : "taskid+projectid"

    portal_task {
        int eid
        int projectid
        varchar taskid PK
        int userid
        int suite_id
        int appid
        varchar package_name
        varchar app_version
        int biz_code
        int syspf_id
    }
    portal_version_condition {
        int id PK
        int eid FK
        int projectid FK
        int appid
        varchar package_name
        int biz_code
        int syspf_id
    }
    task_execute_result {
        int id PK
        varchar task_id FK
        int project_id FK
        int task_status
        int script_total
        int success_script_total
        int fail_script_total
    }
```

**说明**：
- `portal_task` 按 eid 分表（`portal_task_00` ~ `portal_task_99`）
- `portal_task` → `portal_version_condition`：通过 `addTask` MQ 通知异步写入，存储版本检索条件
- `portal_task` → `task_execute_result`：任务完成时 `dealTaskExecuteResult()` 写入执行统计

---

## 五、db_common — 公共数据

```mermaid
erDiagram
    dir_info ||--o{ dir_quartz_job : "dir_id"

    quartz_job ||--o{ quartz_job_log : "job_name"
    quartz_job ||--o| job_task_relation : "job_name"

    server_info ||--o{ server_mount_info : "server_id"
    server_mount_info ||--o{ server_disk_usage_records : "mount_id"

    dir_info {
        bigint id PK
        varchar dir_name
        int dir_type
    }
    dir_quartz_job {
        bigint id PK
        bigint dir_id FK
        varchar job_name
    }
    quartz_job {
        varchar job_name PK
        varchar job_group
        varchar cron_expression
    }
    quartz_job_log {
        bigint id PK
        varchar job_name FK
        int status
    }
    job_task_relation {
        bigint id PK
        varchar job_name FK
        varchar task_id
    }
    server_info {
        int id PK
        varchar server_ip
    }
    server_mount_info {
        int id PK
        int server_id FK
        varchar mount_point
    }
    server_disk_usage_records {
        bigint id PK
        int mount_id FK
        bigint used_size
    }
```

**其他独立表**：`action_log`（活动日志）、`operate_log`（操作日志）

---

## 六、db_case — 用例管理

```mermaid
erDiagram
    case_dir ||--o{ case_info : "dir_id"
    case_info ||--o{ case_step : "case_id"
    case_info ||--o{ case_comment : "case_id"
    case_info ||--o{ case_tag : "case_id"
    case_info ||--o{ case_action_log : "case_id"
    case_info ||--o{ case_revision : "case_id"

    case_info {
        bigint id PK
        int project_id
        bigint dir_id FK
        varchar case_name
        int case_type
    }
    case_dir {
        bigint id PK
        int project_id
        bigint parent_id FK
        varchar dir_name
    }
    case_step {
        bigint id PK
        bigint case_id FK
        text step_content
        int order_num
    }
    case_comment {
        bigint id PK
        bigint case_id FK
        text content
    }
    case_tag {
        bigint id PK
        bigint case_id FK
        varchar tag_name
    }
```

**关系说明**：

| 父表 | 子表 | 关联字段 | 说明 |
|------|------|---------|------|
| `case_dir` | `case_info` | `dir_id` | 目录下的用例 |
| `case_dir` | `case_dir` | `parent_id` | 目录树自引用 |
| `case_info` | `case_step` | `case_id` | 用例的步骤 |
| `case_info` | `case_comment` | `case_id` | 用例的评论 |
| `case_info` | `case_tag` | `case_id` | 用例的标签 |

**独立表**：`case_statistic`（用例统计）、`case_script_no_sync`（脚本未同步追踪）

---

## 七、db_bi — BI 统计打点

```mermaid
erDiagram
    bi_task_dot {
        bigint id PK
        int project_id
        varchar task_id
        int dot_type
    }
    bi_script_dot {
        bigint id PK
        int project_id
        varchar task_id
        int script_id
    }
    bi_pc_task_dot {
        bigint id PK
        int project_id
        varchar task_id
    }
    bi_pc_script_dot {
        bigint id PK
        int project_id
        varchar task_id
    }
    bi_project_script_statistic {
        int id PK
        int project_id
    }
```

全部为独立打点表，无外键关联，按 `project_id` + `task_id` 查询。

---

## 八、db_admin — 管理后台

```mermaid
erDiagram
    admin_role ||--o{ admin_user_role : "role_id"

    admin_role {
        int id PK
        varchar role_name
    }
    admin_user_role {
        int id PK
        int user_id
        int role_id FK
    }
```

---

## 九、db_cross — 跨模块关联

当前库表未文档化（待补充）。

---

## 跨库引用模式

平台基础功能服务 中常见跨库引用方式：

| 模式 | 示例 | 说明 |
|------|------|------|
| `{db}.project_id` 软引用 | `db_plan.plan_info.project_id` → `db_user.project_info.id` | 无 FK 约束，仅值相等 |
| `{db}.eid` 软引用 | `db_portal.portal_task.eid` → `db_user.enterprise_info.id` | 企业隔离键 |
| `{db}.user_id` 软引用 | `db_plan.execute_record.create_user_id` → `db_user.user_info.id` | 创建人/操作人 |

> 注意：这些跨库引用在 MySQL 中没有物理外键约束，依赖应用层代码保证一致性。
