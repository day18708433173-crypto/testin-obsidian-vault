# SQL/db_quartz/ -- 库索引

定时任务库，由 app处理服务 模块通过 MyBatis Mapper 读写。

## 表

| 表名 | 用途 | 详页 |
|------|------|------|
| quartz_job_info | 定时任务/模板信息（cron、设备、脚本、通知配置） | [quartz_job_info](db_quartz/quartz_job_info.md) |
| quartz_job_statement | 定时任务执行流水记录 | [quartz_job_statement](db_quartz/quartz_job_statement.md) |

## 被使用模块

- app处理服务（主要读写）
