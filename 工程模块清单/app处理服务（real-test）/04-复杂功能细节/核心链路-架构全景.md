---
branch: syy.release.z7.8.1.0
type: 专题
tags: [核心链路, 架构, real]
---

# 核心链路-架构全景

> real 云真机平台 4 模块 + 外部服务 + 存储层的架构关系总图。

## 架构全景图

```mermaid
flowchart TB
    subgraph DeviceLayer["设备层"]
        Device["上位机/设备<br/>Android / iOS / PC"]
        WebClient["Web 客户端<br/>Browser / API"]
    end

    subgraph CoreModules["real 云真机平台核心模块"]
        direction LR
        realcfg["real-cfg<br/>配置管理中心<br/><br/>- 平台授权<br/>- DB环境管理<br/>- 项目配置<br/>- 错误信息管理"]
        realcc["real-controlcenter<br/>设备管理中心<br/><br/>- TCP MINA长连接<br/>- 设备注册/管理<br/>- 任务下发<br/>- 协议解析"]
        realsched["real-scheduling<br/>任务调度引擎<br/><br/>- 任务初始化<br/>- 设备匹配<br/>- 结果回收<br/>- 异常处理"]
        realtest["real-test<br/>入口与报告服务<br/><br/>- 任务入口API<br/>- 报告生成<br/>- 定时任务管理<br/>- 通知回调"]
    end

    subgraph ExternalServices["外部服务 & 基础设施"]
        UserMgr["UserManager<br/>用户管理"]
        NoticeMgr["NoticeManager<br/>邮件/机器人"]
        ScriptSvc["ScriptService<br/>脚本服务"]
        FileSvc["FileService<br/>文件服务"]
        PayMgr["PayManager<br/>支付服务"]
        Analysis["RealAnalysis<br/>分析服务"]
    end

    subgraph Storage["数据存储层"]
        MySQL["MySQL<br/>MyBatis"]
        MongoDB["MongoDB<br/>测试结果"]
        ES["Elasticsearch<br/>任务索引"]
        Redis["Redis<br/>MQ/队列/缓存<br/>分布式锁"]
    end

    %% 模块间通信
    realcfg -->|"REST"| realcc
    realcc -->|"RPC"| realsched
    realsched -->|"REST"| realtest
    realtest -->|"RPC"| realsched

    %% 设备通信
    Device -->|"TCP MINA长连接"| realcc

    %% 客户端通信
    WebClient -->|"REST /v3/*"| realtest

    %% 存储访问
    realcfg --> MySQL
    realcfg --> Redis
    realcc --> MySQL
    realcc --> Redis
    realsched --> MySQL
    realsched --> MongoDB
    realsched --> ES
    realsched --> Redis
    realtest --> MySQL
    realtest --> MongoDB
    realtest --> ES
    realtest --> Redis

    %% 外部服务
    realtest --> UserMgr
    realtest --> NoticeMgr
    realtest --> ScriptSvc
    realtest --> FileSvc
    realtest --> PayMgr
    realsched --> Analysis
```

## 模块职责

| 模块 | 职责 | 入口 | 存储 |
|------|------|------|------|
| 平台配置 | 平台授权、DB环境管理、项目配置、错误信息管理 | /* + /v3/* | MySQL + Redis |
| 设备控制中心 | TCP MINA 长连接、设备注册/管理、任务下发、协议解析 | /* + /v3/* + TCP:8000 | MySQL + Redis |
| 任务调度服务 | 任务初始化、设备匹配策略、结果回收解析、异常设备处理 | /* + /v3/* | MySQL + MongoDB + ES + Redis |
| app处理服务 | 任务入口API、报告生成(PDF/Excel)、定时任务管理、通知回调 | /* + /v3/* | MySQL + MongoDB + ES + Redis |

## 通信方式图例

| 方式 | 说明 | 使用场景 |
|------|------|----------|
| REST /v3/* | Spring MVC DispatcherServlet | Web 前端调用、模块间标准 REST |
| RPC | ApiServlet action/op 反射 | 模块间内部 RPC 调用 |
| TCP MINA | 自定义 4B header + JSON body | 上位机长连接 |
| Redis MQ | LPUSH/BLPOP 队列 | 通知系统、协议队列、匹配队列 |

## 相关文档

- [基础设施-双入口路由](基础设施-双入口路由.md) — HTTP 双入口详解
- [核心链路-上位机长连接](../../设备控制中心（real-controlcenter）/04-复杂功能细节/核心链路-上位机长连接.md) — TCP 入口详解
- [基础设施-模块间通信](基础设施-模块间通信.md) — 五种通信方式对比
- [核心链路-任务初始化](核心链路-任务初始化.md) — 完整任务创建流程
- [real 平台总览](公共-real仓总览.md) — 模块架构总览
